/**
 * Validated environment variables. Parsed and validated at startup (see app.config).
 * Use this for typed env access; for app-shaped config use config/app.config.
 */

export { env } from './app.config';
